import * as assert from 'assert';
import { describe, it } from 'mocha';
import { test } from '../src/cashregister';

// TODO
describe('CashRegister', () => {
  describe('test', () => {
    it('should return "abc"', () => {
      assert.strictEqual(test(), "abc");
    });
  });
});
